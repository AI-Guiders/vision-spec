{
  "name": "vision-spec",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Agent-native vision alignment — text screens, sketch web player",
  "scripts": {
    "build:bridge": "dotnet build tools/VisionGdlBridge/VisionGdlBridge.fsproj",
    "test": "npm run build:bridge && node --test parser/parser.test.js parser/graph.test.js parser/mental-model.test.js parser/gdl-bridge.test.js",
    "play": "npm run build:bridge && node scripts/vision-play-server.mjs"
  },
  "license": "MIT",
  "dependencies": {
    "vis-network": "^10.1.2"
  }
}
